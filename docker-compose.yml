services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: elodemo
      POSTGRES_USER: elodemo
      POSTGRES_PASSWORD: elodemo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U elodemo"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
